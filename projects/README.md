# Projects

Future code projects go here.

Each project should live in its own folder, for example:

`projects/<project-name>/`

Do not place Obsidian knowledge files here. The knowledge vault lives in
`../knowledge/`.
